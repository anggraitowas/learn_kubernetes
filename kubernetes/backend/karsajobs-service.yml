apiVersion: v1
kind: Service
metadata:
  name: karsajobs
spec:
  type: NodePort

  selector:
    app: karsajobs

  ports:
    - name: http
      port: 8080
      targetPort: 8080

      # Port eksternal yang digunakan browser/frontend.
      nodePort: 30080
